# 星绘智愈 — 孤独症儿童适配改进规划

## 背景

当前产品的核心技术链路已跑通（算法拆解 → 交互作画 → LLM 观察报告），但 UI/交互层的设计仍偏向"普通用户"思维。孤独症谱系障碍（ASD）儿童在感官处理、认知模式、情绪调节、运动能力方面有显著特殊性。如果不做针对性适配，产品可能在真实场景中**引发焦虑而非疗愈**。

本文档基于 ASD 儿童的循证干预方法（TEACCH 结构化教学、Social Story 社交故事、感觉统合理论），规划具体的产品改进方案。

---

## 一、核心问题诊断

### 1.1 感官层面

| 现状 | ASD 儿童的反应 | 风险 |
|------|---------------|------|
| 高饱和多色 UI（黄/粉/紫/绿） | 视觉过载，无法聚焦 | 回避使用 |
| 精灵跳动 + 粒子爆炸动画 | 不可预期的视觉刺激→惊恐 | 情绪崩溃 |
| 随机切换的表扬文案 | 信息处理负担增大 | 困惑、不安 |
| 画面元素密集（工具栏+精灵+进度+气泡） | 注意力分散，找不到关键信息 | 无法执行任务 |

### 1.2 认知层面

| 现状 | ASD 儿童的反应 | 风险 |
|------|---------------|------|
| 6画家×5画×5情绪×4密度的自由选择 | 决策瘫痪（选择越多越焦虑） | 卡在选择页无法开始 |
| "沿着金色虚线画"等抽象指令 | 不理解"沿着"的含义 | 不知道该怎么做 |
| 没有流程预告（不知道下一步是什么） | 对未知的恐惧→拒绝参与 | 放弃使用 |
| "画得真棒"等抽象表扬 | 不理解什么是"棒"，无法建立因果 | 反馈失效 |

### 1.3 运动层面

| 现状 | ASD 儿童的反应 | 风险 |
|------|---------------|------|
| 匹配阈值 80px（统一标准） | 精细运动障碍的儿童无法达标 | 反复失败→挫败 |
| 笔触宽度/路径都需要精确跟踪 | 手眼协调困难 | 连续失败后崩溃 |
| 没有"画了就算"的兜底 | 尝试后得不到正反馈 | 习得性无助 |

### 1.4 情绪层面

| 现状 | ASD 儿童的反应 | 风险 |
|------|---------------|------|
| 匹配失败只有"再试试" | 没有情绪疏导路径 | 积累挫败 |
| 无情绪崩溃预警机制 | 系统不知道孩子已经烦躁了 | 错过干预时机 |
| 无退出/暂停的明确路径 | 不知道可以停下来 | 被迫继续→meltdown |

---

## 二、改进方案详细设计

### 2.1 安静模式（Calm Mode）

**目标**：提供一个感官友好的低刺激界面选项。

**设计规范**：

```
┌─ 安静模式视觉规范 ─────────────────────────────┐
│                                                │
│  背景色：#FAFAFA（近白）或 #F5F0EB（暖白）      │
│  主文字：#333333                                │
│  辅助色：#999999                                │
│  强调色：仅1种，柔和蓝 #7BA7CC                  │
│  圆角：保留（圆角=安全感）                       │
│                                                │
│  动画：全部禁用，仅用 opacity 0→1 淡入          │
│  过渡：300ms ease，不弹跳                       │
│  精灵：静态图（不跳动），缩小到 40px             │
│  粒子：全部移除                                  │
│  字体：更大（base 16px→18px），行高 1.8          │
│                                                │
│  信息密度：每屏最多 3 个操作元素                  │
│  声音：默认静音，可选白噪音                      │
│                                                │
└────────────────────────────────────────────────┘
```

**切换方式**：
- 进入 app 时的"模式选择"页（而非设置里埋着）
- 两个大按钮：「彩色冒险模式」「安静舒适模式」
- 配图说明，不需要阅读文字就能理解

**技术实现**：
- 全局 Context 存储模式状态
- CSS 变量切换（`data-theme="calm"`）
- 条件渲染动画组件（calm 模式下返回静态元素）

**开发工时**：1 天

---

### 2.2 分层难度系统

**目标**：适配不同运动能力和认知水平的儿童。

**三个层级**：

| 层级 | 名称 | 匹配阈值 | 笔触数量 | 失败处理 | 适合人群 |
|------|------|---------|---------|---------|---------|
| Level 1 | 小小画家 | 任何触碰画布=通过 | 15-25 笔 | 不存在失败 | 重度运动障碍/3-5岁 |
| Level 2 | 小画家 | 150px 以内 | 40-80 笔 | 3次后自动跳过 | 轻中度/5-8岁 |
| Level 3 | 小艺术家 | 80px 以内 | 全部笔触 | 提示重试 | 高功能/8岁以上 |

**Level 1 的特殊设计**：
- 引导线区域更大（高亮整个"区域"而非线条）
- 画面只显示 15 笔大块笔触（roughness=4）
- 每画完一笔→整个区域被美丽的 AI 笔触填充（即时满足感）
- 没有分数判定，只有"哇，你画了一笔！"

**选择时机**：
- 由家长/治疗师在首次启动时设置
- 存入 localStorage，不让儿童自己改
- 可设置密码保护

**开发工时**：0.5 天

---

### 2.3 视觉时间表（Visual Schedule）

**目标**：让儿童提前知道整个活动的流程和当前进度。

**设计**：

```
画布左侧固定展示（垂直步骤条）：

  ✅ 选画    ← 已完成（灰色+勾）
  ✅ 选颜色
  ── ── ──
  🔵 第 3 笔  ← 当前（高亮+动画圈）
  ○  第 4 笔  ← 下一步（可见但未激活）
  ○  第 5 笔
  ⋮
  ○  第 15 笔
  ── ── ──
  ○  看我的画  ← 终点（明确的结束标志）
```

**关键原则**（TEACCH 方法）：
- 从上到下，从已完成到未完成
- 当前步骤用明确的**不同颜色/大小**标识
- 未来步骤可见但不可操作（减少不确定性）
- **明确的终点**："还有 X 步就完成了"

**对高功能 ASD 儿童的变体**：
- 可显示具体数字："3/15"
- 可显示预估时间："大约还有 2 分钟"

**开发工时**：1 天

---

### 2.4 先看后做（Watch-Then-Do）

**目标**：在每一笔让用户画之前，先演示一遍。

**流程状态机**：

```
每一笔的状态：

[等待] → [演示中] → [你的回合] → [完成]
  │         │           │          │
  │    精灵沿路径      画布高亮    ✓ 动画
  │    飞行1.5s       等待触摸
  │    （可跳过）
  │
  └─ 点击"下一笔"触发
```

**演示方式**：
- 精灵（或一个小光点）从路径起点匀速移动到终点
- 路径上留下半透明的"轨迹"（即引导线）
- 移动速度约 1.5 秒/笔，固定节奏
- 演示完后画布出现明确的"你的回合！"提示

**为什么重要**：
- ASD 儿童是"视觉模仿学习者"
- "先观察完整动作→再模仿"是应用行为分析（ABA）的核心策略
- 减少"我不知道该往哪画"的焦虑

**可选**：提供开关让高功能儿童关闭演示直接画。

**开发工时**：1 天

---

### 2.5 具象化反馈系统

**目标**：用 ASD 儿童能理解的方式给予反馈。

**替换规则**：

| 抽象表扬（删除） | 具象反馈（替代） | 原理 |
|--------------|--------------|------|
| "画得真棒！" | "你画了一条线，从这里到这里。" | 描述事实 |
| "太厉害了！" | "今天第 5 笔了！比昨天多 2 笔。" | 数字对比 |
| "好漂亮的一笔！" | "你用了蓝色。梵高也喜欢蓝色。" | 建立关联 |
| "继续加油！" | "还剩 4 笔。画完就可以看你的画了。" | 明确预期 |
| 粒子爆炸动画 | 进度条安静地增长 1 格 | 可预期的变化 |

**数据驱动的个性化反馈**：
- 系统记录儿童对哪种反馈后继续画的概率更高
- 逐渐增加有效反馈的比例
- 如："小明对数字型反馈响应率 85%，对描述型 62%" → 多用数字型

**安静模式下的反馈**：
- 仅一个 ✓ 出现在完成的步骤旁
- 无文字、无声音、无动画
- 极简主义，对极度敏感的儿童最安全

**开发工时**：0.5 天

---

### 2.6 情绪识别与平静策略

**目标**：被动检测挫败情绪，主动提供调节支持。

**检测信号**（全部来自现有的 pointer event 数据）：

| 信号 | 数据来源 | 阈值 |
|------|---------|------|
| 连续匹配失败 | guide-system | ≥ 3 次 |
| 笔触突然加重 | pressure 数据 | pressure > 0.8 持续 |
| 笔触速度骤增 | 相邻点时间差 | < 10ms/点（乱划） |
| 长时间不动笔 | 犹豫时间 | > 20 秒 |
| 连续点击跳过 | skip 事件 | ≥ 3 次连续 |
| 反复快速触碰 | pointerdown 频率 | > 5次/秒（拍打行为） |

**触发后的「平静协议」**：

```
第 1 级（轻度）：连续 2 次失败
  → 精灵温柔提示："慢慢来，不着急"
  → 自动降低匹配阈值（临时放水）

第 2 级（中度）：连续 4 次失败 / 检测到乱划
  → 画布变暗，出现选择：
    「继续画」「休息一下」「换一幅画」
  → 如果选休息：显示呼吸引导动画
  
第 3 级（重度）：20秒不动 + 之前连续失败
  → 自动进入平静页面
  → 缓慢的蓝色呼吸圆圈（吸气膨胀4s，呼气收缩6s）
  → 轻柔白噪音（可选）
  → 不限时，儿童自主决定何时返回
  → 返回后自动跳过当前笔触，从新的开始
```

**呼吸引导设计**（循证：4-7-8 呼吸法简化版）：

```
  ┌──────────────────────┐
  │                      │
  │      ◯ → ◉ → ◯      │  圆圈4秒膨胀，6秒收缩
  │                      │
  │    "吸气...呼气..."   │  可选文字/可关闭
  │                      │
  └──────────────────────┘
```

**开发工时**：1.5 天

---

### 2.7 社交故事（首次引导）

**目标**：首次使用前用"社交故事"消除焦虑。

**内容**（6 页图文）：

```
第 1 页：[画画工具图]
  "今天我们要用平板画画。"

第 2 页：[精灵图]
  "有一个星星朋友叫 Starry，它会帮我。"

第 3 页：[手指在屏幕上画线的图]
  "我会用手指在屏幕上画线。"

第 4 页：[引导线图]
  "屏幕上会出现金色的线，我跟着它画。"

第 5 页：[失败→重试的图]
  "如果画不好，没关系。我可以再试，或者跳过。"

第 6 页：[完成的画+笑脸]
  "画完后，我能看到自己的画！准备好了吗？"
```

**设计原则**（Carol Gray 社交故事标准）：
- 使用描述句（"我会..."）而非指令句（"你必须..."）
- 每页一个信息点
- 配合简单的图片/图标
- 肯定句比否定句多（不说"不要害怕"，说"没关系"）

**呈现方式**：
- 首次进入 `/create` 时弹出
- 左右滑动翻页
- 每页停留至少 3 秒再显示"下一页"按钮
- 看完后不再出现（localStorage 记录）
- 家长可在设置中重新触发

**开发工时**：0.5 天

---

### 2.8 纵向追踪与治疗师看板

**目标**：把单次观察报告积累为长期趋势，提供给治疗师。

**数据模型**：

```typescript
interface ChildProfile {
  id: string;
  name: string;
  age: number;
  diagnosisLevel: 'level1' | 'level2' | 'level3';
  sessions: PaintingSession[];    // 历史所有 session
  preferences: {
    calmMode: boolean;
    difficulty: 1 | 2 | 3;
    preferredArtists: string[];   // 常选的画家
    preferredColors: string[];    // 常用颜色
  };
}
```

**趋势分析维度**：

| 维度 | 计算方式 | 含义 |
|------|---------|------|
| 专注度趋势 | avg(完成笔触数/总笔触数) per session | 参与意愿变化 |
| 犹豫度趋势 | avg(waitTimeMs) per week | 自信心变化 |
| 色彩丰富度 | unique(颜色类别) per session | 情感表达范围 |
| 偏好稳定性 | 连续选同一画家的次数 | 固着行为观察 |
| 完成意愿 | 主动点完成 vs 中途退出 | 抗挫能力变化 |
| 情绪触发频次 | 平静协议触发次数/session | 情绪调节进展 |

**治疗师看板内容**：
- 近 30 天的趋势图（折线图）
- AI 生成的周度/月度综合观察
- 标注异常点（如"某天突然大量使用黑色"）
- 对比同龄 ASD 儿童的参考范围（脱敏后的群体数据）

**存储方案**：
- 短期（demo阶段）：导出为 JSON/PDF 发邮件给治疗师
- 长期（产品化）：Supabase + 治疗师账号体系

**开发工时**：3 天（含后端）

---

### 2.9 重复与节奏机制

**目标**：把 ASD 儿童的"重复行为"特质转化为正向体验。

**「每日一画」模式**：
- 允许同一幅画反复画，每次记录为独立 session
- 多次画同一幅后生成"进化对比"：第1次 vs 第5次
- 不评判好坏，只展示变化："你这次用了更多颜色" / "这次画得更快了"

**「节奏模式」**：
- 开启后每 X 秒自动推进到下一笔（X 可调：2/3/5 秒）
- 配合简单的节拍音（tick...tick...tick...）
- 利用 ASD 儿童对规律/节奏的偏好
- 如果没赶上节奏→自动 AI 补画→不中断节奏感

**「系列收集」**：
- 画完某画家全部 5 幅→获得收集徽章
- 画完某种情绪全部 5 幅→获得"情绪探索者"徽章
- 连续 7 天画画→获得"坚持小画家"
- 利用部分 ASD 儿童的**收集/整理偏好**

**开发工时**：1 天

---

### 2.10 多感官反馈通道

**目标**：为不同感觉偏好的 ASD 儿童提供多通道反馈。

| 通道 | 实现 | 适合人群 | 默认状态 |
|------|------|---------|---------|
| 视觉 | ✓ 出现 / 进度条变化 | 视觉学习者 | 开 |
| 触觉 | Vibration API 短震动 | 本体觉寻求者 | 关 |
| 听觉-语音 | TTS 读出反馈文字 | 听觉学习者 | 关 |
| 听觉-音效 | 完成音（温柔"叮"） | 听觉偏好者 | 关 |
| 听觉-白噪音 | 背景雨声/海浪声 | 需要遮蔽环境噪音的 | 关 |
| 音乐节奏 | 与笔触同步的节拍 | 节奏敏感型 | 关 |

**关键**：全部默认关闭，由家长/治疗师根据个体特征开启。

**颜色-声音联觉选项**：
- 红色→低音 do
- 橙色→re
- 黄色→mi
- 绿色→fa
- 蓝色→sol
- 紫色→la

部分 ASD 儿童有联觉倾向，把画画变成"作曲"能显著提升兴趣。

**开发工时**：2 天

---

## 三、实施优先级与排期

### P0 — 核心无障碍（不做等于产品对 ASD 儿童不可用）

| 项目 | 工时 | 产出 |
|------|------|------|
| 安静模式 | 1天 | 全局 Context + CSS 变量 + 条件渲染 |
| 分层难度 | 0.5天 | 难度配置 + 匹配阈值调整 + 自动跳过 |
| 具象化反馈 | 0.5天 | 重写 MESSAGES 数据 + 数据驱动模板 |
| **P0 合计** | **2天** | |

### P1 — 结构化支持（显著提升 ASD 儿童体验）

| 项目 | 工时 | 产出 |
|------|------|------|
| 视觉时间表 | 1天 | VisualSchedule 组件 + 状态同步 |
| 先看后做 | 1天 | 演示动画状态机 + 跳过开关 |
| 情绪检测+平静策略 | 1.5天 | 行为检测器 + 平静页面 + 呼吸动画 |
| 社交故事 | 0.5天 | 首次引导组件 + 6页图文 |
| **P1 合计** | **4天** | |

### P2 — 增强体验（锦上添花，长期价值）

| 项目 | 工时 | 产出 |
|------|------|------|
| 重复/节奏模式 | 1天 | 节拍系统 + 每日一画 + 徽章 |
| 治疗师看板 | 3天 | Supabase 后端 + 趋势图 + AI 周报 |
| **P2 合计** | **4天** | |

### P3 — 远期规划

| 项目 | 工时 | 产出 |
|------|------|------|
| 多感官通道 | 2天 | 触觉/听觉/联觉模块 |
| 个性化反馈学习 | 2天 | 反馈效果追踪 + 策略调整 |
| **P3 合计** | **4天** | |

---

## 四、技术架构变更

### 新增全局状态

```typescript
interface AppSettings {
  calmMode: boolean;        // 安静模式
  difficulty: 1 | 2 | 3;   // 难度等级
  watchBeforeDo: boolean;   // 先看后做
  rhythmMode: boolean;      // 节奏模式
  rhythmBPM: number;        // 节拍速度
  sensory: {
    vibration: boolean;
    tts: boolean;
    soundEffects: boolean;
    whiteNoise: 'off' | 'rain' | 'ocean' | 'forest';
    synesthesia: boolean;   // 颜色-声音联觉
  };
  childProfile: {
    name: string;
    age: number;
    difficultyLocked: boolean;  // 家长锁定
  };
}
```

### 新增文件

```
src/
├── contexts/
│   └── SettingsContext.tsx      # 全局设置 Context
├── components/
│   ├── VisualSchedule.tsx      # 视觉时间表
│   ├── CalmBreathing.tsx       # 呼吸引导动画
│   ├── SocialStory.tsx         # 社交故事
│   ├── WatchDemo.tsx           # 先看后做演示
│   └── DifficultyGate.tsx      # 家长设置难度的入口
├── lib/
│   ├── emotion-detector.ts     # 情绪状态检测
│   ├── feedback-engine.ts      # 具象化反馈生成
│   └── rhythm-timer.ts         # 节奏模式计时器
└── app/
    ├── settings/page.tsx       # 家长设置页
    └── dashboard/page.tsx      # 治疗师看板（P2）
```

---

## 五、对比赛评委的话术建议

在 PPT/答辩中可以这样呈现：

> "我们不是简单地做了一个无障碍开关。我们研究了 TEACCH 结构化教学法的'视觉时间表'原则、Carol Gray 的社交故事方法论、以及感觉统合理论中的感官分级策略，将循证的特殊教育干预方法**原生融入了产品交互设计**中。
>
> 比如我们的'先看后做'机制对应的是 ABA（应用行为分析）中的'模仿前先观察完整示范'策略；我们的'平静协议'对应的是情绪调节中的'觉察-暂停-呼吸'三步法；我们的'具象化反馈'对应的是 ASD 儿童'字面理解'的认知特点。
>
> 这不是技术的堆砌，是对用户真实需求的深度回应。"

---

## 六、伦理与安全红线

1. **永远不做诊断** — 系统只"观察"和"记录"，不输出任何诊断结论
2. **永远不惩罚** — 没有失败状态、没有扣分、没有负面反馈
3. **永远可退出** — 任何时候都能停止，不强制完成
4. **数据归属家长** — 儿童数据由家长完全控制，可随时删除
5. **不比较** — 不做儿童之间的排名/对比
6. **不标签化** — 不在界面上出现"孤独症""障碍"等词汇
7. **专业背书** — 报告页注明"本内容不构成医学建议，请咨询专业治疗师"
