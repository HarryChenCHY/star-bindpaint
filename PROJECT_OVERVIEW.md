# 星迹智绘 StarTrace · 产品与研究说明

## 1. 产品概述

星迹智绘把已有的笔触拆解算法转化为零基础绘画入门工具。用户可以选择示例作品、上传参考图，或进入自由画布。系统将复杂图像转化为有序笔触，用户跟随星点和星迹逐步完成作品；月亮伙伴负责解释下一步、提示进度并在完成后点亮作品星图。

产品不面向特定疾病、年龄段或教育阶段，不提供医疗或心理结论，也不使用活动、学校、模型厂商或算法团队作为产品背书。

## 2. 世界观与信息架构

| 概念 | 产品含义 | 主要界面 |
|---|---|---|
| 星点 | 单步起点、方向和颜色提示 | 画板 |
| 星迹 | AI 生成的有序笔触路径 | 画板 |
| 月亮伙伴 | 逐步绘画向导 | 创作入口、画板、完成页 |
| 星愿 | 每日可完成的小练习 | 首页、星图 |
| 星图 | 作品与练习收藏 | 画廊 |
| 点亮星星 | 完成作品或一次练习 | 完成页、画廊 |

五步 AI 辅助绘画：选择参考 → AI 拆解 → 跟随星迹 → 完成绘画 → 查看反馈。

## 3. 核心交互

### 3.1 三种入口

1. 精选临摹：从作品库选择参考图。
2. 上传图片：在浏览器压缩后生成星迹。
3. 自由星域：选择主题支架或直接自由绘画。

### 3.2 三档引导

- 完整星迹：路径、起点、方向与颜色均可见。
- 平衡星迹：保留关键星点与主要路径。
- 轻量星迹：降低外部提示，支持独立练习。

### 3.3 人工与 AI 的边界

系统分别记录亲手尝试、亲手通过、未通过、跳过与自动续画笔触。内容覆盖率表示画面完成程度，不等同于学习程度；自动完成也不计为用户真实动笔。

## 4. 零基础绘画者专项设计

- 用单笔任务代替“面对整幅画”的启动压力。
- 默认提供明确起点和方向，不要求先理解专业术语。
- 随时切换提示等级，支持从依赖提示到较少提示。
- 自动续画可用来观察完整笔触顺序，但在数据中与亲手笔触分开。
- 自由主题从圆形、直线、曲线、色块等基础形态开始。
- 反馈只呈现可观察事实和下一次可执行建议，不给天赋或能力标签。

## 5. 技术能力

### 5.1 笔触拆解算法

输入图像在客户端完成缩放、颜色采样、结构分层与笔触排序，输出每条笔触的控制点、宽度、颜色、层级和顺序。产品层将其映射为当前星迹、星点、进度和自动演示。

关键价值不只是把图像“画得像”，而是把结果变成可跟随、可暂停、可分段学习的动作序列。

### 5.2 Diffusion 图像生成

用户主动点击“AI 星光变换”后，当前画布会临时发送到服务端图像生成适配器。系统要求保留构图与形状，只改变画面质感。该功能是完成后的可选扩展，不替代绘画练习，也不计入亲手完成笔触。

### 5.3 LLM 学习反馈

反馈首先由确定性规则计算关键指标，保证数据口径稳定；可选 LLM 只负责把同一组已归一化指标改写为简短自然语言。请求不包含作品图片、坐标、颜色或身份信息，并明确禁止心理、健康、人格和因果推断。

## 6. 数据与隐私

浏览器本地保存作品、练习记录和界面偏好。匿名研究数据和作品云保存均默认关闭并分别授权。授权研究后生成随机参与编号；服务端只接收指标白名单。

默认研究字段包括：匿名编号、会话编号、研究阶段、模式、引导等级、计划笔触、亲手尝试/通过/未通过、AI 笔触、跳过笔触、首次动笔时延、持续时间和完成状态。

明确排除：姓名、联系方式、原图、作品图片、作品云路径、逐笔坐标与颜色。用户可以撤回授权，并按匿名编号删除对应云端记录与云端作品。

## 7. 研究属性

研究核心不是证明某个生成模型更强，而是评估“有序笔触拆解 + 渐进提示”作为产品机制能否：

1. 缩短零基础绘画者的首次动笔时延；
2. 提高一次练习中的实际动笔与完成概率；
3. 提升 7—14 天内的练习次数与活跃天数；
4. 在减少提示后仍保留部分构图和笔触顺序学习效果。

具体实验、变量与论文结构见 [RESEARCH_PROTOCOL.md](./RESEARCH_PROTOCOL.md)。

## 8. 系统模块

```text
参考图 / 自由主题
        ↓
笔触拆解与顺序规划
        ↓
星点提示 → 星迹跟画 → 月亮伙伴反馈
        ↓               ↘ 可选图像生成
作品星图 ← 本地保存 ← 学习反馈
        ↘ 经同意的匿名研究指标
```

关键代码：

- `src/lib/stroke-engine.ts`：笔触拆解。
- `src/lib/guide-system.ts`：顺序、判定与提示状态。
- `src/components/PaintCanvas.tsx`：画布与用户笔迹。
- `src/lib/painting-tracker.ts`：本地交互指标。
- `src/lib/learning-feedback.ts`：确定性学习反馈。
- `src/lib/privacy-settings.ts`：同意、匿名编号与删除控制。
- `src/app/api/analytics/*`：白名单入库和受保护统计。

## 9. 产品边界

- 单次完成和画面覆盖不能独立证明绘画能力提升。
- AI 自动续画增加的是画面覆盖，不是用户动笔频次。
- 匹配分数只用于当前交互提示，不作为能力排名。
- 研究结论必须来自预注册或明确的实验设计与统计分析，而非产品演示数据。
