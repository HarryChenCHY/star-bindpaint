# 星迹智绘 StarTrace

> 项目级协作上下文。每次新对话先读取本文件。

## 2026-09-16 参与测试流程简化

- 参与者五步：进入→熟悉工具→第一轮→第二轮→完成对比。隐藏批次选择，后台以setting/enrollment-target指定已发布批次；默认预试，旧参与者继续原批次。
- 练习图自动规划。取消准备轮次按钮，前测卡点击开始即保存并启动ResearchDrawing，定位到画板；刷新已保存前测时保留手动开始，避免静默启动新任务。休息到时只展示第二轮开始卡片。
- 结束直接报告；三题访谈改后台ResearcherInterview记录，来源/时间入报告，修改需原因且有审计；旧参与者填写记录保留来源兼容。
- 未改时限、问题、ABBA分配、评分/导出或代画禁用。构建、29项单元测试和完整浏览器A/B与B/A/离线重试/唯一开始/实际落笔/报告/评分/研究者访谈/权限/手机布局通过。验收说明与截图：论文/测试流程简化验收/。

## 2026-09-16 产品介绍的算法与风格来源

- `/intro` 详细展示三代算法、方案A五遍交互扫描与莫奈真实阶段图、颜色拟合公式、重排叠色实验、同图工程指标。`AlgorithmExplanation.tsx`、`intro-sources.ts` 管理说明与4篇论文原文/DOI。
- 六种风格说明由 `StyleExplanation.tsx` 提供：馆藏参照、感性解释、运行参数、曲线与局限。现有参数是人工预设，没有画家笔迹测量数据；新增资料是回溯对照，不能声称是最初选值的来源。taper不使用widthVariation，毕加索周期断笔与伦勃朗15%干笔缺口是项目联想，不是艺术史统计。
- 预览资产为原试验输出，放在 `public/intro/painting-order/`；未改变笔刷或研究数据。构建、ESLint与 `tests/study/intro-algorithm.mjs` 通过，说明和截图在 `论文/产品介绍算法溯源验收/`。

## 2026-09-16 自动续画暂停与速度

- 暂停由独立 `autoPaused` 控制，保留自动模式、粉色选中态与速度工具；播放按钮恢复当前笔触，只有主动点击沿星迹/自由星域才切换模式。教程打开也仅暂停。
- 自动续画取消旧批量加速逻辑，逐笔按200–1000ms间隔播放，默认1000ms。教程说明同步。

## 2026-09-16 方案 A 已接入

- 用户采用方案 A，默认规划器升级为 `layered-raster-3-five-pass`：五遍固定笔宽30/16/8/4/2.2（短边200），按3/4/5/6/6网格逐行扫描，每遍重新拟合覆盖区颜色；1000预算分60/120/220/280/320次，其他预算按比例分配，无收益跳过。取消旧的前300轮随机缩宽描述。
- 体验提示卡与规划进度使用实际遍次；产品介绍同步。历史研究材料不改写，新研究材料标记新版本并需重新审核。
- 莫奈实测998笔，与已批准A试验逐笔一致；验收见 `论文/方案A接入验收/实施说明.md`。新增 `tests/study/layered-raster.mjs` 和预算/顺序测试。构建、29项测试、Worker取消重试、手机布局与撤销回归通过。本地未推送发布。
- 旧探索基线冻结于 `experiments/painting-order/baseline-planner.ts`，实验脚本使用该文件，避免生产算法更新污染对照。

## 2026-09-16 PDF 修改意见落实

- 追加“沿星迹”撤销：与自由画板共用最近 30 次画布快照，跟画快照附带序列进度与作品计数恢复；手绘、换一笔、伙伴补笔、暂停后的整段自动续画可撤销，“一起画”整组撤回。移除跟画推进及伙伴补笔延迟，避免撤销后旧定时器跳笔。保留真实尝试次数和会话时长。`follow-undo.mjs` 与 `undo-progress.test.ts` 验证画布、进度、计数；构建、ESLint、原有 26 项测试和新增计数测试通过。

- 追加细节：笔触提示的数字圆圈、字号、方向线、箭头与虚线均按笔宽和端点间距同比缩放，移除固定最小大圆圈；短笔触保留缩小的 1/2 与箭头，三档提示保持。四种尺寸×三档 Canvas 视觉检查、构建与 ESLint 通过。

- 已逐页阅读桌面 `修改意见 0916.pdf` 的 7 页文字和配图，8 项意见均已实现；计划和验收见 `论文/0916修改计划与验收.md`，截图见 `论文/0916验收/`。
- 首页右侧 `HomePaintingDemo` 展示上传→笔触序列→画笔跟随的循环示意；全局底栏“世界”改“介绍”；选图默认上传并排最左。
- 体验引导用 30% 紫色区域、虚线外框、数字 1/2 和箭头；保留三档提示。`matchScore` 改为点到线段距离并增加容错，正式研究冻结规则不改。
- 自动续画的模式按钮在播放时变为暂停，点击后返回跟画，重启接续进度。
- 颜色第十格新增 HSV 色环、S/V 方框和数值输入。笔宽的用户设置优先保留；绘图引擎重建恢复颜色宽度，落笔回调稳定以避免反馈更新打断画笔。
- 底栏最右“教程”打开 `PaintTutorial` 六章交互说明；打开时暂停自动绘画，示例不修改当前作品。图片、提示卡、工具、三种模式与保存流程均有说明。
- 26 项单元测试、构建、ESLint、`revisions-0916.mjs`、既有体验/颜色浏览器检查通过。改动仅本地，未提交推送与发布。

## 2026-09-11 腾讯云 CVM 部署

- 用户已授权整个前后端部署腾讯云和本地 Git 推送后自动更新。新目标为 CVM `1.13.169.247`（TencentOS 3.3），SSH 主机 ED25519 指纹 `SHA256:9L3VtBtAue5eeFcFPVWTo80GHXlu3nnjVPI+iux+OQY` 已通过控制台核验。
- `startrace.service` 使用 Podman 运行 Next.js 页面和 API；绑定 `127.0.0.1:3000`。`startrace-update.timer` 每分钟检查 GitHub main 并执行 `/usr/local/sbin/startrace-deploy`，先测试、构建、隔离健康检查，再停写备份和切换。失败保留或恢复上一镜像。
- 正式数据 `/srv/startrace/studies`，应用内 `STUDY_DATA_DIR=/app/data/studies`；旧版统计和云作品保存使用 `STARTRACE_OBJECT_DATA_DIR=/app/data/studies/objects`，无需 OSS。口令只在服务器 `/etc/startrace.env`，禁止输出其内容。更新前备份在 `/srv/startrace/backups`，目前是同盘备份。
- 部署脚本、服务单元和域名模板见 `deploy/`；服务器安装的管理脚本或单元修改后需维护者同步安装。main 的应用提交会自动更新，研究数据与材料不会随 Git 复制。
- 备案仍在审核。当前通过 SSH 隧道 `ssh -N -L 3010:127.0.0.1:3000 root@1.13.169.247` 在 `http://localhost:3010` 预览云端。待实际备案号与证书确认后再启用域名 HTTPS；旧 CloudBase 默认地址仍指向旧服务，不自动切换，不删除旧环境。
- 云端新研究库没有自动发布材料或正式研究；真人预试与材料审核仍由研究者执行。完整运维与回滚说明见 `deploy/README.md`。

## 2026-09-10 研究版改造

- 用户已审核实施计划并授权“开始按计划步骤改造”；按 `论文/星迹智绘_程序改造实施计划_待审核.md` 的 P0—P7 推进，阶段交付用于预览，常规实施不重复询问授权。
- 当前新增 `/study` 全流程、`/admin/studies` 研究后台、`/rater` 双人评分、`/simplify` 1000 笔效果预览（接回原 `/paint`）。详细状态与使用说明见 `论文/程序改造_实施记录与使用说明.md`。
- 本轮研究聚焦同一参与者、同一参考图的 AB/BA 单次任务比较；长期练习不是本次主要实验终点。
- 研究共享画布为 `src/components/study/StudyCanvas.tsx`，领域逻辑在 `src/lib/study/`，新预算规划在 `src/lib/budget-strokes.ts`；旧区域规划保留用于历史兼容。
- 本地研究数据库/作品目录为 `data/studies/`，禁止提交。生产需配置持久 `STUDY_DATA_DIR`，使用 Node >=22.13；环境凭据只读 `.env.local` 或平台环境，不在回答、截图或日志中展示。
- 新增 `npm run test:study` 与 `npm run test:study:browser`。后者使用 3002 端口和临时隔离数据库；合成夹具不得进入真人预试。
- 软件工程预览不等于 P6 真人预试或 P7 正式研究完成。正式材料审核、6 人预试和实际设备验证必须由真实参与者与研究者执行。
- P6 已新增 `/admin/studies/materials` 和 `npm run test:study:materials`，12 张材料工程检查通过；问题记录在 `论文/P6_材料检查/检查结果.md`。下一优先项：点状计划与有效落笔判定的一致性、细线保留。当前不能宣称 P6 已全部完成。

## 最新算法方向（用户已明确修改原计划）

- 体验画板提示卡在锁定栏下方显示当前参考原图（ReferencePreview），按宽度适配、保持比例，点击使用原生 dialog 全屏查看；点击画面、关闭按钮或 Esc 返回卡片。不影响已有画布，纯自由画布无参考图时不显示该区域。

- 自由绘画已彻底移除主题选择弹窗、主题步骤卡、换主题返回分支和对应组件；直接进入或切换自由星域都直接画画，返回按钮离开至选图。画笔数字在底栏与大小弹层用 Math.round 显示整数，绘制宽度保持原始精度。回归脚本 experience-controls.mjs 覆盖两个自由入口与整数显示。

- 首页主创作按钮放大为黄色高对比按钮，三项价值说明以下的长介绍通过原生 details 默认收起。`/intro` 已重做 10 章可视化产品说明，含可收缩侧栏、实际笔刷示例、真实 Worker 笔触回放、A/B 条件示意、论文原文链接与实现边界。不要把旧 Hertzmann 曲线算法描述为当前默认，也不要把六种风格预设说成训练过的大师模型。
- `FloatingCompanion.tsx` 统一体验画板两模式的提示卡：默认锁定，解锁后可拖动手柄或用方向键移动，减号收为方圆图标，图标可展开且解锁时可拖动。位置/锁定偏好本地保存，窗口变化时约束可见范围。验证脚本 `tests/study/intro-panels.mjs`，截图目录 `论文/产品介绍与浮动面板验收/`。

- 最新交互：选图与设置页移除引导强度/颗粒度选择，新画默认完整引导与细节档（roughness 1，算法版本后缀 fine），画板内保留完整/适度/起点切换，终点为方向箭头。底栏自由模式不再锁定，可与已有星迹往返切换；没有参考计划时回选图入口。首页删两个指定按钮，产品研究改为产品介绍。
- 参与测试不再校验固定 STUDY_ENROLLMENT_CODE，改为自定义唯一 researchCode（1–64 个文字/数字/下划线/短横线；NFKC、去首尾空白、忽略大小写判重）。一份码对应同一人两条件配对，跨批次和重启后均保留去重；研究者后台列出所有已有测试，报告、复盘和导出使用研究码。原材料发布门槛与入组上限保留，旧记录用原编号显示。验收见 `论文/交互简化与研究码验收/`。

- P7 交接准备工具已接入研究工作台：材料、完整计划、协议问卷与评分快照私有保存到 `handoffs/`，支持历史下载、文件 SHA-256 校验及备份。`scripts/verify-study-handoff.mjs` 可核验交接包；18 项自动测试及完整浏览器流程通过。导出不自动发布研究，真实六人预试仍待执行。研究者口令已按用户指示统一到本地环境配置并重启验证；旧统计后台因其存储未配置仍返回 503。说明见 `论文/P7_研究版本交接使用说明.md`。

- P6 任务负担复盘已接入六人看板：两条件用时/超时/落笔/停留/评分中位数、配对用时差、逐人明细和指导去重统计；空值不填零。复盘 JSON 私有保存到 `pilot-reviews/`，支持最近十份重下载，备份与撤回清理均覆盖。17 项测试、浏览器全流程、报告字节一致性及备份哈希校验通过。真实预试仍未代做。

- P6 下一步已接入 `/admin/studies/pilot` 六人预试看板：实际流程/双人评分汇总、问题修订与复验说明、计划哈希绑定复核、JSON 下载。正式发布后端增加六人完整与评分、当前复核和无阻断问题检查。15 项自动测试及看板手机端/全流程回归通过；指南见 `论文/P6_六人预试执行指南.md`。真实预试仍须实际执行。

- 本轮用户已采用新算法，进入 P6 软件整合：`stroke-config.ts` 统一参数；`painting-client.ts` / `painting.worker.ts` 让原画板后台计算、显示进度并可取消重试。研究后台显示旧/新材料版本、哈希、任务负担并下载预试执行卡。Word 见 `论文/星迹智绘_1000笔算法原理解析与P6程序整合.docx`；生成器在 `scripts/docs/generate_algorithm_doc.py`，依赖 python-docx。
- P6 本轮验收记录见 `论文/P6_整合验收/`；14 个自动测试、Worker 取消重试和研究全流程通过。`node tests/study/planning.mjs` 针对本地 3001 验证。六人真人预试仍未完成，不可标记 P7 正式研究已完成。

- 最新追加：默认与上限改为 1000 笔，分析尺寸升至 256；前 300 轮建立整体，后续以细笔修整。200 笔版本仅作对照，历史材料不自动迁移。当前验证见 `论文/1000笔算法验证/`。

- 不再要求“轮廓 → 大色块 → 小色块”，新生成序列统一按笔触顺序绘画，预算上限 1000，不截取旧结果。`decomposeImage` 默认新算法，`decomposeImageLegacy` 仅工程对照。
- 新测试材料 `phase: paint`，参考图保持原图。历史三阶段材料、已存记录不自动改写；正式使用前在后台重新生成、审核材料。
- `/simplify` 展示白纸上的真实算法合成效果与旧算法对照；体验默认 .85 透明度，研究规划匹配 StudyCanvas 的不透明画笔。不能以像素色差证明美感或真人学习效果。
- 实测图片、说明见 `论文/1000笔算法验证/`（旧结果保留在 200 笔目录）；`node tests/study/paint-budget.mjs` 在 3001 预览上验证。

## 项目定位

星迹智绘是面向零基础绘画者的研究型 AI 辅助绘画应用。已有笔触拆解算法把参考图转化为有序“星迹笔触”，产品通过绘画星点、月亮伙伴、每日星愿和作品星图降低第一笔门槛，并记录真实动笔与持续练习。

- 一句话：沿着星迹，一笔一笔画出自己的世界。
- 目标：研究有序笔触拆解与渐进提示对首次动笔、单次完成和 7—14 天练习频次的影响。
- 边界：面向一般零基础绘画者，不按疾病、年龄或教育阶段限定；不提供医疗、心理、人格或天赋结论；不在产品界面展示活动、院校、供应商或团队背书。
- 工作目录：`E:\star-bindpaint`

## 技术栈与关键模块

- Next.js 16 App Router、React 19、TypeScript、Tailwind CSS 4、framer-motion。
- `src/lib/stroke-engine.ts`：图像笔触拆解和顺序规划。
- `src/lib/guide-system.ts`：星迹进度、判定和提示状态。
- `src/components/PaintCanvas.tsx`：画布、亲手笔迹和自动笔触。
- `src/app/paint/page.tsx`：画板主流程。
- `src/lib/painting-tracker.ts`：本地会话指标。
- `src/lib/learning-feedback.ts`：确定性学习反馈。
- `src/lib/practice-store.ts`：本地练习频次。
- `src/lib/privacy-settings.ts`：研究同意、匿名编号与删除。
- `src/app/api/analytics/*`：白名单研究数据与受保护后台。

项目同时安装 `framer-motion` 与 `motion`；自写代码统一使用 `framer-motion`。

## 视觉语言

- Neo-Brutalist Cartoon：白/浅灰底、深蓝黑 `#17233F` 描边、硬阴影、较大圆角。
- 主色：星光黄 `#FFD166`、星迹紫 `#6558D9`、薄荷绿 `#69D2C2`、星云粉 `#FF8FAB`、浅紫 `#ECEAFE`。
- 所有主流程视觉围绕星点、星迹、月亮伙伴、星愿、星图与点亮星星。
- 移动端必须保持 390px 视口无横向溢出；底部 Dock 不得遮住页面最后一项内容。

## 数据与研究边界

- 匿名研究数据和作品云保存默认关闭并分别授权。
- 默认研究记录不得包含姓名、原图、作品图片、云路径、逐笔坐标或颜色。
- 内容覆盖率不等于学习效果；自动笔触不等于真实动笔；匹配分数不作为能力评分。
- 正式研究条件由研究者分配；自然使用数据保持 `studyCondition: unassigned`。
- 研究后台必须配置 `ANALYTICS_ADMIN_TOKEN`，不得在客户端、日志或文档中写入真实口令。
- 所有凭据只来自本地或部署平台环境变量，绝不提交到 Git。

## 代码风格

- 2 空格缩进；TS/JS 单引号，JSX 属性双引号。
- 路径别名 `@/*` 指向 `./src/*`。
- App Router 客户端组件在文件顶部声明 `'use client';`。
- 优先使用 Tailwind utility；复杂品牌样式可用 inline style。
- 只为不明显的设计原因写简短中文注释。
- 搜索优先使用 `rg`；本地文件修改使用 `apply_patch`。
- 保留用户已有改动和 `conversation-export.json`，不要清理无关文件。

## 本地预览与发布

- 研究接口依赖 `node:sqlite`。曾因 3000 开发服务用 Node 22.12.0 返回 HTML 500，已切换至 22.16.0；npm dev/build/start 增加运行环境检查。当前可用 Node 路径 `/private/tmp/node-v22.16.0-darwin-arm64/bin`；重新启动时须使用 >=22.13.0 且支持 SQLite 的 Node。研究请求增加非 JSON 故障提示，参与页支持重试；19 项自动测试及故障恢复浏览器检查通过。
- 本地地址：`http://localhost:3001`。
- 验收顺序：TypeScript → ESLint → production build → 桌面/390px 浏览器检查 → 用户确认。
- 未经用户查看并确认本地版本，不提交、不推送、不覆盖云端。
- 云环境 ID：`startrace-d5gbndi213c162cc5`。
- Cloud Run 服务：`startrace`，容器端口 `3000`。
- 公网地址：`https://startrace-306119-11-1416317231.sh.run.tcloudbase.com/`。
- 保留 `next.config.ts` 的 `output: 'standalone'` 和根目录 `Dockerfile`。
- Git 身份：陈皓宇 `<HarryChenCHY@users.noreply.github.com>`；远端 `git@github.com:HarryChenCHY/star-bindpaint.git`。
- 部署上下文来源：根目录 `conversation-export.json`，该文件属于用户，不提交或改写。

## 六阶段重构状态

### 2026-09-10 自由画笔颜色与功能清理

- 颜色预览、工具栏色块、实时自由笔触、六种风格笔刷和喷绘统一使用 `brush-color.ts` 的 HSV 调色，饱和度与亮度限制在有效范围后转换 RGB。
- 六种自由笔刷以笔形、纹理体现风格；取消额外饱和度增益，色相扰动不超过 2°，普通自由笔触不透明度为 0.96。引导绘画的原有不透明度不变；喷绘边缘仍受点密度和透明度影响。
- 删除“AI 星光变换”按钮、生成与结果界面、图片生成 API 和对应设置；保留作品保存与文字反馈。
- 验证：22 项研究/颜色单元测试、生产构建、自由画笔颜色浏览器采样及手机绘画切换回归通过。浏览器颜色测试为 `tests/study/free-color.mjs`，在白色及有色背景上检查笔触中心与预览的偏差。

- ✅ 阶段 1：首页、导航、元信息和研究型产品介绍。
- ✅ 阶段 2：精选临摹、上传图片、自由画布与三档引导入口。
- ✅ 阶段 3：星点/星迹画板、月亮伙伴、完整原始笔触、可暂停自动续画和星星魔法棒。
- ✅ 阶段 4：每日星愿、七天动笔、连续天数、练习次数和作品星图。
- ✅ 阶段 5：可解释学习反馈、人工/AI/覆盖口径、首次动笔和匿名研究数据后台。
- ✅ 阶段 6：旧定位清理、隐私授权、匿名删除、接口白名单、后台保护、安全响应头、文档与双端验收。

## 大型任务工作流

满足任一条件即视为大型任务：修改 5 个以上文件、涉及多个模块联动，或要求整体/完整重构。

1. 开始前给出完整阶段计划并等待确认。
2. 每次只执行一个主阶段，完成后汇报并等待继续。
3. 汇报格式：`✅ 步骤 N/M 完成：[完成内容] → 下一步：[下一阶段]，是否继续？`
4. 超过 5 分钟的任务持续提供简短进度更新。

如果上下文压缩连续失败 3 次，停止并提示用户开启新对话，不继续追加内容。
